# Telegram-бот для управления сменами (AIogram)

Версия: 3.0 (Финальная)

## Описание

Telegram-бот для автоматизации управления сменами исполнителей. Бот собирает данные пользователей через онбординг, управляет их доступностью, автоматически добавляет в рабочие группы и позволяет записываться на смены.

## Технологии

- **Python 3.9+**
- **AIogram 3.1.1** (асинхронный фреймворк для Telegram Bot API)
- **SQLAlchemy 2.0+** (ORM)
- **SQLite/PostgreSQL** (база данных)
- **asyncio** (асинхронное программирование)

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd StaffManagerBot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка окружения

Создайте файл `.env` в корне проекта (скопируйте из `.env.example`):

```env
# Telegram Bot Token (получить у @BotFather)
BOT_TOKEN=your_bot_token_here

# Database URL
# Для SQLite (по умолчанию):
DATABASE_URL=sqlite+aiosqlite:///./staff_bot.db
# Для PostgreSQL:
# DATABASE_URL=postgresql+asyncpg://user:password@localhost/dbname

# Admin Chat IDs (можно несколько через запятую)
ADMIN_CHAT_IDS=123456789,987654321

# Work Group ID (рабочий чат, можно узнать через @userinfobot)
WORK_GROUP_ID=-1001234567890

# Notification Channel ID (канал уведомлений)
NOTIFICATION_CHANNEL_ID=-1001234567890
```

### 4. Получение необходимых ID

#### Получение BOT_TOKEN:
1. Найдите бота [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен

#### Получение Chat ID:
1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Добавьте его в нужный чат/канал или начните с ним диалог
3. Скопируйте нужный ID

**Важно для групп:**
- ID группы имеет отрицательное значение (начинается с `-100`)
- Бот должен быть администратором в рабочем чате с правом "Приглашать пользователей"

## Запуск

```bash
python main.py
```

## Структура проекта

```
StaffManagerBot/
├── main.py                      # Главный файл запуска бота
├── config.py                    # Конфигурация и настройки
├── requirements.txt             # Зависимости Python
├── .env                         # Переменные окружения (создать вручную)
├── README.md                    # Документация
│
├── database/
│   ├── __init__.py
│   ├── models.py               # Модели базы данных (User, Shift, etc.)
│   ├── database.py             # Настройка подключения к БД
│   └── crud.py                 # CRUD операции с БД
│
├── handlers/
│   ├── __init__.py
│   ├── states.py               # FSM состояния
│   ├── validators.py           # Валидаторы данных
│   ├── user_handlers.py        # Обработчики для пользователей
│   └── admin_handlers.py       # Обработчики для администраторов
│
└── scheduler/
    ├── __init__.py
    └── weekly_update.py        # Планировщик еженедельных обновлений
```

## Функциональность

### Для пользователей

#### 1. Онбординг (первая регистрация)
При первом запуске `/start` бот собирает:
- ✅ ФИО (полностью)
- ✅ Навыки (свободный текст)
- ✅ Опыт работы (количество смен)
- ✅ Курс обучения (1-5)
- ✅ Контактный телефон
- ✅ Предпочтительные дни недели для работы

После регистрации пользователь **автоматически** добавляется в:
- Канал уведомлений
- Рабочий чат (через приглашение)

#### 2. Управление сменами
- 📋 Просмотр доступных смен
- ✅ Запись на смену
- 📝 Просмотр своих записей
- ❌ Отмена записи (для будущих смен)

#### 3. Обновление доступности
- 🔄 Ручное обновление через меню
- 📅 Автоматический запрос каждое воскресенье

### Для администраторов

Доступ через команду `/admin` (только для пользователей из `ADMIN_CHAT_IDS`)

#### 1. Управление сменами
- ➕ Добавление новых смен (дата, время, описание)
- 📝 Редактирование существующих смен
- 🗄️ Архивирование смен

#### 2. Управление пользователями
- 📋 Просмотр полного списка пользователей
- ⭐ Изменение рейтинга пользователей (1-5)
- Начальный рейтинг для новых пользователей: **3 звезды**

#### 3. Настройки системы
- 💬 Установка Work Group ID
- 📢 Установка Notification Channel ID

#### 4. Рассылка
- 📢 Отправка сообщений всем зарегистрированным пользователям
- Поддержка текста, фото, видео, документов

## База данных

### Таблицы

- **users** - Пользователи (ФИО, навыки, опыт, курс, телефон, дни, рейтинг)
- **shifts** - Смены (дата, описание, статус)
- **shift_assignments** - Записи пользователей на смены
- **settings** - Настройки системы

База данных создается автоматически при первом запуске.

## Рейтинговая система

- **Начальный рейтинг**: 3 звезды (для всех новых пользователей)
- **Диапазон**: 1-5 звезд
- **Изменение**: Администратор может вручную изменить рейтинг
- **Автоматическая логика**: Структура готова для автоматического пересчета на основе отработанных смен (MVP)

## Еженедельное обновление

Каждое **воскресенье в 10:00** бот автоматически отправляет всем зарегистрированным пользователям запрос на обновление доступности на следующую неделю.

## Требования к настройке бота

### Права администратора в рабочем чате

Для корректной работы добавления пользователей в рабочий чат:
1. Добавьте бота в группу как администратора
2. Убедитесь, что у бота есть право **"Приглашать пользователей"**
3. Установите `WORK_GROUP_ID` в `.env`

### Права в канале уведомлений

1. Добавьте бота в канал как администратора
2. Установите `NOTIFICATION_CHANNEL_ID` в `.env`

## Разработка

### Добавление нового функционала

1. **Новые хендлеры** - добавьте в `handlers/user_handlers.py` или `handlers/admin_handlers.py`
2. **Новые модели БД** - добавьте в `database/models.py`, затем пересоздайте БД
3. **Новые CRUD операции** - добавьте в `database/crud.py`

### Логирование

Логи выводятся в консоль с уровнем INFO. Для отладки измените уровень на DEBUG в `main.py`:

```python
logging.basicConfig(level=logging.DEBUG)
```

## Решение проблем

### Бот не отвечает
- Проверьте правильность `BOT_TOKEN`
- Убедитесь, что бот запущен (`python main.py`)
- Проверьте логи на наличие ошибок

### Пользователи не добавляются в группу
- Убедитесь, что бот - администратор группы
- Проверьте наличие права "Приглашать пользователей"
- Проверьте правильность `WORK_GROUP_ID` (должен начинаться с `-100`)

### Ошибки базы данных
- Проверьте правильность `DATABASE_URL`
- Для PostgreSQL убедитесь, что установлен драйвер `asyncpg`
- Проверьте права доступа к БД

## Лицензия

Проект создан в соответствии с техническим заданием.

## Контакты

Для вопросов и предложений обращайтесь к разработчику.

